#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <stdio.h>

int main(int argc, char const *argv[])
{
    int scoreCount, a;
    for ( scoreCount; scanf("%d", &a) == 1; scoreCount++);
    return 0;
}
